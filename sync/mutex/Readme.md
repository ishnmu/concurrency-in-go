# Mutex

- _Mutex_ stands for `mutual exclusion` and is a way to guard **critical sections** in our program.

- **Critical Sections** are nothing but an area in our code/program that requires exclusive access to a shared resource. 